When a task is complete in BaseUI, at minimum run relevant verification commands. Preferred baseline: .\\gradlew.bat test for unit tests and .\\gradlew.bat jar when build/package confidence matters. For rendering/UI behavior changes, also consider .\\gradlew.bat runClient for manual verification in the Forge client. Avoid claiming render-path changes are complete without checking both tests and a client render path when practical.